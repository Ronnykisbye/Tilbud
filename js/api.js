const Api={async geocodePlace(query){const clean=String(query||"").trim();if(!clean)throw new Error("Skriv en adresse eller et sted.");const dawaHit=await this.geocodeDawa(clean);if(dawaHit)return dawaHit;const osmHit=await this.geocodeNominatim(clean);if(osmHit)return osmHit;throw new Error("Stedet blev ikke fundet. Prøv et mere præcist stednavn.")},async geocodeDawa(query){for(const baseUrl of[APP_CONFIG.dawaAdresseUrl,APP_CONFIG.dawaAdgangUrl]){try{const url=`${baseUrl}?${Utils.qs({q:query,struktur:"mini",per_side:1})}`;const response=await fetch(url,{headers:{Accept:"application/json"},cache:"no-store"});if(!response.ok)continue;const data=await response.json();const hit=Array.isArray(data)?data[0]:null;if(hit?.x&&hit?.y)return{label:hit.betegnelse||query,lat:Number(hit.y),lon:Number(hit.x),source:baseUrl.includes("adgang")?"DAWA adgangsadresse":"DAWA adresse"}}catch(error){console.warn("DAWA-fejl:",error)}}return null},async geocodeNominatim(query){try{const url=`${APP_CONFIG.nominatimUrl}?${Utils.qs({q:`${query} Denmark`,format:"jsonv2",limit:1,addressdetails:1})}`;const response=await fetch(url,{headers:{Accept:"application/json"},cache:"no-store"});if(!response.ok)return null;const data=await response.json();const hit=Array.isArray(data)?data[0]:null;if(!hit?.lat||!hit?.lon)return null;return{label:hit.display_name||query,lat:Number(hit.lat),lon:Number(hit.lon),source:"OpenStreetMap/Nominatim"}}catch(error){console.warn("Nominatim-fejl:",error);return null}},buildKbUrl(center,radiusMeters,itemsPerPage){const radius=Utils.clamp(radiusMeters,50,APP_CONFIG.maxRadiusMeters);const box=Utils.bbox(center,radius);const bbo=[box.west,box.north,box.east,box.south].join(",");return`${APP_CONFIG.kbApiBase}?${Utils.qs({bbo,itemsPerPage})}`}};